<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :post_title %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_area :url, id: :post_url %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_area :author, id: :post_author %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
